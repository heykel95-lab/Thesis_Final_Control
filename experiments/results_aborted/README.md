# Aborted archives

Run directories kept out of `results/` so no analysis script counts them, and
kept at all so the campaign's failures stay part of its record.

- `P2_t1_pos_m040_r02_20260817` — aborted while moving to `q_init` with
  `joint_motion_generator_acceleration_discontinuity`. The trial never started
  and no per-cycle CSV was written. Re-recorded on 2026-08-19 under a
  byte-identical parameter set; the replacement is `results/P2_t1_pos_m040/r02`.
